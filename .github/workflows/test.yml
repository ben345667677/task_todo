name: Test and Deploy Task TODO App

on: [push, pull_request]

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Run Test
      run: |
        docker compose up -d
        sleep 30
        docker compose ps
        if docker compose run --rm test_client python server_test.py; then
          echo "✅ SUCCESS: Test passed with code 200"
        else
          echo "❌ FAILED: Test failed - no code 200"
          exit 1
        fi
        docker compose down
    
    - name: Login to Docker Hub
      if: success()
      run: |
        echo "✅ Logging in to Docker Hub..."
        echo "dckr_pat_WN5PD44wCjUnAWcqBwTqZgJH__4" | docker login -u "ben1234561423424" --password-stdin
    
    - name: Build and Push Task TODO App Image
      if: success()
      run: |
        echo "🏗️ Building Task TODO App Docker image..."
        docker build -t ben1234561423424/task-todo-app:latest .
        
        echo "📤 Pushing to Docker Hub..."
        docker push ben1234561423424/task-todo-app:latest
        
        echo "✅ Task TODO App successfully pushed to Docker Hub!"
        echo "📍 Image available at: docker.io/ben1234561423424/task-todo-app:latest"
    
    - name: Verify Docker Push Success
      if: success()
      run: |
        echo "🔍 Verifying image was pushed successfully..."
        docker pull ben1234561423424/task-todo-app:latest
        echo "✅ Verification successful! Task TODO App is available on Docker Hub"
    
    - name: Deployment Failed
      if: failure()
      run: |
        echo "❌ Deployment failed! Task TODO App was not pushed to Docker Hub"
        echo "🔧 Check the logs above for errors"